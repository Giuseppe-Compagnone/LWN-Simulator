title: GetDeviceRequest
type: object

properties:
  id:
    type: string
    x-go-name: ID
    x-go-json-ignore: true
    x-oapi-codegen-extra-tags:
      validate: "required,len=36"
      uri: "id"

required:
  - id
